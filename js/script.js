$('.bas').slick({
    dots: true,
    infinite: true,
    speed: 500,
    fade: true,
    cssEase: 'linear'
  });
    
$('.withOptions').slick({
    dots: true,
    arrows: true,
    autoplayspeed: 1000,
    centerMode: true,
});